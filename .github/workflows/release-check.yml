name: Release version check

on:
  push:
    branches: [main]
    tags: ["v*"]

jobs:
  check-versions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check manifest versions match CHANGELOG.md
        run: ./scripts/check-release-versions.sh
