description = "Advance an idea's lifecycle status in the vault."
prompt = """
You are operating on RM's Obsidian memory vault. Read GEMINI.md (this extension's context file) for the vault location and path-resolution rules before acting.

Follow this procedure. If `skills/vault-idea-status/SKILL.md` exists in this extension directory, read it for the full algorithm and follow it precisely; otherwise use the summary below.

Use the `vault-idea-status` skill to update an idea's `**Status:**` in
`ideas/{domain}.md` and move it between active and parked/archived
sections when warranted.

Argument: the idea and its new state, e.g.
- `/vault-idea mark the offline-sync idea as building`
- `/vault-idea the X idea shipped`
- `/vault-idea park the Y idea`

Matches the file's existing status vocabulary. Confirms before moving an
idea between sections.

User request / arguments: {{args}}
"""
