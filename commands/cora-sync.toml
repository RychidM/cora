description = "Regenerate agent files (CLAUDE.md, GEMINI.md, ...) for a project repo."
prompt = """
You are operating on the user's Obsidian memory vault. Read GEMINI.md (this extension's context file) for the vault location and path-resolution rules before acting.

Follow this procedure. If `skills/cora-project-sync/SKILL.md` exists in this extension directory, read it for the full algorithm and follow it precisely; otherwise use the summary below.

Use the `cora-project-sync` skill to write fresh agent context files
into a project repo from the vault's `AGENTS.md` and the project's
`OVERVIEW.md`.

Argument: a repo path. If not given, use the current working directory
or the path registered for the named project.

User request / arguments: {{args}}
"""
