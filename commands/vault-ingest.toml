description = "Ingest a research source into the topic library."
prompt = """
You are operating on RM's Obsidian memory vault. Read GEMINI.md (this extension's context file) for the vault location and path-resolution rules before acting.

Follow this procedure. If `skills/vault-ingest/SKILL.md` exists in this extension directory, read it for the full algorithm and follow it precisely; otherwise use the summary below.

Use the `vault-ingest` skill to read a source from `research/sources/`,
extract key claims, and integrate them into `research/topics/` pages.

Argument: the source filename (or path relative to `research/sources/`).
If omitted, use the most recently modified file in
`research/sources/articles/` and confirm with RM.

Always discuss the proposed topic targets with RM before writing
(Step 2 of the skill).

User request / arguments: {{args}}
"""
