description = "Show current vault status — projects, pending entries, open issues."
prompt = """
You are operating on RM's Obsidian memory vault. Read GEMINI.md (this extension's context file) for the vault location and path-resolution rules before acting.

Follow this procedure. If `skills/vault-status/SKILL.md` exists in this extension directory, read it for the full algorithm and follow it precisely; otherwise use the summary below.

Use the `vault-status` skill to produce a concise summary of vault
state. Read-only — never writes to any file.

User request / arguments: {{args}}
"""
